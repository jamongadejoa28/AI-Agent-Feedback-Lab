# AI-Agent-Feedback-Lab 개발 규칙

본 저장소는 `First-AI-Agent`의 응답 품질 평가 및 사용자 피드백 수집을 담당하는 평가 전용 서비스입니다. 상위 저장소인 `../ai-agent`의 기본 개발 규칙을 계승하며, 아래의 원칙을 엄격히 준수합니다.

---

## 1. 코드 작성 및 한글 주석 규칙

- 새로 작성하거나 수정하는 모든 Python 모듈, 클래스, 함수, 주요 분기문에는 역할, 입력값, 반환값, 발생 가능한 예외 및 부작용을 명확히 설명하는 **상세한 한글 주석/독스트링**을 작성한다.
- 단순한 코드 번역식 주석은 지양하고, 해당 구현을 선택한 이유, 데이터 흐름, 비즈니스 의도와 주의사항을 중심으로 기술한다.
- 코드 동작이 변경될 경우 관련 주석과 문서도 반드시 함께 갱신하여 정합성을 유지한다.

---

## 2. 정적 분석 및 테스트 실행 순서 규칙

- 코드 변경 후에는 반드시 `.venv/bin/pyright .` 정적 타입 검사를 먼저 수행한다.
- **Pyright 검사에서 오류가 1건이라도 존재하면 pytest를 절대 실행하지 않는다.**
- Pyright 결과 `0 errors`를 확인한 후에만 pytest 단위/통합 테스트를 실행한다.
- 테스트 실패 수정 또는 리팩토링으로 Python 코드가 변경되었다면, 다음 테스트 실행 전에 반드시 Pyright 검사를 다시 수행하여 0 오류 상태를 재확인한다.

---

## 3. 아키텍처 경계 및 구현 원칙

- **내부 모듈 직접 Import 금지**: `../ai-agent`의 내부 모듈(예: `rag_pipeline.graph` 등)을 직접 import하지 않는다. First-AI-Agent와의 통신은 오직 HTTP API(`POST /v1/query`)를 통해서만 수행한다.
- **Agent 지능 복제 및 수정 금지**: Feedback Lab은 First-AI-Agent의 응답을 가공, 보정, 요약, 재작성하지 않으며, Agent의 실제 응답(`answer` 필드)을 그대로 기록하고 화면에 표시한다.
- **SQLite 단일 진실 공급원(SSOT)**: 동시성 환경에서 데이터 신뢰성을 보장하기 위해 SQLite WAL(Write-Ahead Logging) 모드와 명시적 트랜잭션을 적용한다.
- **요청 선예약(Reservation)과 멱등성**: 중복 요청 및 Agent 이중 호출을 방지하기 위해, Agent를 호출하기 전에 상태를 `processing`으로 선예약한다.
- **완전한 테스터 격리**: 익명 세션 쿠키(`feedback_tester_id`)를 발급하여 다른 사용자의 테스트 및 피드백에 접근하거나 조회할 수 없도록 제한한다.

---

## 4. 문서와 구현의 동기화 규칙

- 계획 중인 기능이나 후속 로드맵(Phase 4~9)을 이미 구현된 것처럼 문서에 기술하지 않는다.
- 현재 구현 범위는 **Phase 1~3 MVP**에 한정하며, 미구현 기능은 명확히 `계획` 또는 `미구현`으로 표기한다.
- Git 커밋 해시나 브랜치 명칭을 문서 본문에 동결 기준으로 기재하지 않는다.
